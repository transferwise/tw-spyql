machine:
  java:
    version: oraclejdk8
  services:
      - docker

dependencies:
  override:
    - ./gradlew compileTestJava --console=plain

test:
  override:
    - ./gradlew test --console=plain
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - find . -type f -regex ".*/build/test-results/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \;

deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
      - mkdir $CIRCLE_ARTIFACTS/build
      - ./gradlew spyql-core:publish --console=plain
      - mv spyql-core/build/libs/*.jar $CIRCLE_ARTIFACTS/build
      - ./gradlew spyql-rx:publish --console=plain
      - mv spyql-rx/build/libs/*.jar $CIRCLE_ARTIFACTS/build
  all:
    branch: /.*?/
    commands:
      - mkdir $CIRCLE_ARTIFACTS/build
      - ./gradlew spyql-core:publish --console=plain
      - mv spyql-core/build/libs/*.jar $CIRCLE_ARTIFACTS/build
      - ./gradlew spyql-rx:publish --console=plain
      - mv spyql-rx/build/libs/*.jar $CIRCLE_ARTIFACTS/build
